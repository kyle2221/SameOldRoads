# ---- Frontend (.env at repo root) ----
# Vite reads VITE_* vars at build time.

# Where the SameOldRoads API lives. Leave empty to use /api (recommended,
# lets Vite proxy to the local server in dev — see vite.config.js).
VITE_API_BASE=/api
